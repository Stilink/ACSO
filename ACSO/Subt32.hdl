CHIP Subt32 {
	IN a1[16], a2[16],
	b1[16], b2[16];
	OUT res1[16], res2[16];
	PARTS:
		Complement2(in1=b1, in2=b2, out1=notb1, out2=notb2);
		Add32(a1=a1, a2=a2, b1=notb1, b2=notb2, sum1=res1, sum2=res2);
}